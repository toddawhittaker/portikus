/** Zod schemas for the WebSocket and cross-process event streams: lifecycle, terminal, filesystem, and Git state (STACK.md section 5). Placeholder; arrives in a later epic. */
export const packageName = "@portikus/events";
