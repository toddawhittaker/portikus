# Portikus worker — managed by Ansible.
[Unit]
Description=Portikus Worker
After=network.target postgresql.service portikus-controller.service
Requires=postgresql.service
ConditionPathExists=/var/lib/portikus/app/apps/worker/dist/index.js

[Service]
Type=simple
User=portikus
Group=portikus
EnvironmentFile=/etc/portikus/worker.env
EnvironmentFile=-/etc/portikus/worker.override.env
ExecStart=/usr/bin/node /var/lib/portikus/app/apps/worker/dist/index.js
Restart=on-failure
RestartSec=5

# Hardening (SPEC section 24)
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
IPAddressAllow=127.0.0.0/8
IPAddressDeny=any

[Install]
WantedBy=multi-user.target
