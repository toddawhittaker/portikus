# Portikus mock identity provider environment — managed by Ansible.
# Test only (ADR 0008).
NODE_ENV={{ portikus_node_env }}
LOG_LEVEL={{ portikus_log_level }}
MOCK_OIDC_PORT={{ portikus_mock_idp_port }}
MOCK_OIDC_ISSUER={{ portikus_public_url }}/mock-idp
MOCK_OIDC_CLIENT_ID=portikus-dev
MOCK_OIDC_CLIENT_SECRET={{ portikus_mock_client_secret }}
MOCK_OIDC_REDIRECT_URI={{ portikus_public_url }}/auth/callback
