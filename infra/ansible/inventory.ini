# Ansible inventory for the pilot platform VM.
# The IP is assigned by the libvirt DHCP network; update it after
# `tofu apply` reports the VM address, or use the DNS name if one
# is configured.
[platform]
portikus ansible_host=REPLACE_ME ansible_user=deploy
