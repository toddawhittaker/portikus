# Ansible inventory for the pilot platform VM.
# The address is read from the PORTIKUS_VM_IP environment variable, which
# `make configure-vm` sets from the OpenTofu output. Nothing here needs
# editing per deployment.
[platform]
portikus ansible_host="{{ lookup('env', 'PORTIKUS_VM_IP') }}" ansible_user=deploy
