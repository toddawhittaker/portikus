# Copy to terraform.tfvars (git-ignored) and fill in your values.
# Every other variable has a default in variables.tf.

# Contents of your SSH public key file, e.g. ~/.ssh/id_ed25519.pub.
ssh_public_key = "ssh-ed25519 AAAA... you@example.com"
