# SOPS configuration (STACK.md section 27).
# Add your age public key here before encrypting secrets.
#
# Generate a key pair:  age-keygen -o infra/secrets/age-key.txt
# Then set:  export SOPS_AGE_KEY_FILE=infra/secrets/age-key.txt
#
# creation_rules:
#   - path_regex: '\.enc\.(yaml|json)$'
#     age: 'REPLACE_ME_WITH_AGE_PUBLIC_KEY'
