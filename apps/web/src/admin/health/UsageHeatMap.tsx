import type { HealthSeries } from "@portikus/contracts";
import type { ChartFrame } from "./charts/scales.js";

/** Stub until Epic 19 T3b fills it; renders nothing. */
export function UsageHeatMap(_props: { series: HealthSeries; frame: ChartFrame }) {
	return null;
}
