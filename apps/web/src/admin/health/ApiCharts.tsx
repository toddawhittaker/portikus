import type { HealthSeries } from "@portikus/contracts";
import type { ChartFrame } from "./charts/scales.js";

/** Stub until Epic 19 T4 fills it; renders nothing. */
export function ApiCharts(_props: { series: HealthSeries; frame: ChartFrame }) {
	return null;
}
